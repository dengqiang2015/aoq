#include <stdio.h>

int ml = 0;
int fr = 0;

void debug(const char *str, const char *file, int line, const char *func, int act)
{

	//(act == 1) ? ml++ : fr++;
	//printf("note:%s, FILE:%s, LINE:%d, FUNCTION:%s, ACTION:%d\n", str, file, line, func, act);
	return;
}
